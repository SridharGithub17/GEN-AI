This is an conversational chatbot an genai poc for TD_Bank.

Requirements:
1. you need to be having the data setup in your local postgres.
2. Install the requirements.txt

Once it is done, just run streamlit run app.py 
